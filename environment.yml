name: hypoforge
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - langgraph>=0.2.0
      - pydantic>=2.0
      - pyyaml>=6.0
      - rich>=13.0
      - python-dotenv>=1.0
      - langchain-openai>=0.2.0
      # Optional Track A / demo / development dependencies are documented
      # in requirements.txt and should be enabled only when needed.
