name: hypoforge
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - langgraph>=0.2.0
      - pydantic>=2.0
      - pyyaml>=6.0
      - rich>=13.0
      - python-dotenv>=1.0
      - langchain-openai>=0.2.0
      - pypdf>=5.0
      - rank-bm25>=0.2.2
      - pytest>=8.0
      - pytest-asyncio>=0.23
