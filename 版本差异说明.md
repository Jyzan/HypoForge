# 综合版与同学最新版差异说明

## 版本关系

- 对比基线：`309e88a`（`integration/generalized-evidence-ui-v2` 分支上同学的最新版）。
- 本综合版直接以该提交为父提交新增一个普通提交，不改写、不压缩、不覆盖既有 Git 历史。
- 本地 `.env`、运行输出、缓存和知识图谱运行数据均不进入提交。

## 继承同学版本且保留的内容

### M4

- 保留任务契约注入：生成和修复假设时都会读取 M1 的实体 ID、精确名称、别名、必需状态和需求 ID。
- 保留字面约束与语言一致性要求，减少假设没有覆盖原问题实体而被硬校验拒绝的情况。

### M5

- 完整保留同学最新版提示词：方案语言与原问题一致。
- 实体使用与方案语言匹配的名称或别名；避免英文方案中直接混入中文实体名。
- 关键任务术语不能被改写到无法进行字面追踪。

### M6 和通用框架

- M5、M6 的核心执行逻辑沿用同学最新版，本提交不重新设计其算法。
- 保留同学加入的网页断点重试、运行历史侧栏和配置文件整合。

## 本综合版相对同学最新版的主要差异

### M1：使用我们已验证的英文检索输入

- 子问题、领域标签、关系描述和规范实体名称必须使用科学英文。
- `source_mention` 仍保存原问题中的原文片段，用于证明实体确实来自原问题。
- 不采用同学本轮新增的“双语 alias”方案，避免非英文别名重新进入 M2 检索输入。
- 实体来源检查以原问题的 `source_mention` 为依据，不再要求英文规范名称逐字出现在中文原问题中。

### M2：保留我们的多源检索与容错增强

- PubMed、Semantic Scholar、OpenAlex、arXiv 按独立来源执行，不再使用 Semantic Scholar 失败后伪装成 OpenAlex 的回退链路。
- 按子问题生成少量补充检索概念；失败时降级为空，不阻断正式检索。
- 将实体分为必须/非必须检索项并规划有限轮次；结果过少时最多追加一次 must-only 补救轮。
- 保留全文解析重试、arXiv/PDF 获取修复、摘要降级和全文失败原因归类。
- 保留论文、知识条目、来源统计、Evidence ID 和检索过程的完整导出接口。
- 不采用同学本轮新增的“低相似实体再次判定并合并规范实体表”逻辑；M2/M3 继续使用我们此前验证过的实体规范化实现。

### M3：增加可审计的最终图谱实体合并

- M3 完成图谱构建和 grounding 后，对最终实体节点执行相似度合并，默认阈值为 `0.92`。
- 精确同名实体直接合并；语义相似实体使用 complete-linkage，避免链式误合并。
- 合并节点会继承双方的边、别名、元数据和 Evidence/entry 追踪信息。
- 保存合并前后的实体名称、相似度、代表节点、边重定向、重复边和自环处理数量，网页可以审查每一组合并。
- Embedding 不可用时只降级到精确同名合并，不让 M3 因模型或网络问题失败。

### M4：在同学任务契约增强上叠加候选不足容错

- 第一次生成的有效假设少于配置数量时，只额外调用一次生成器。
- 合并两次结果时按假设陈述去重，并处理模型重复使用 `H1` 等 ID 的情况。
- 第二次仍不足时接受较少的候选继续执行，不再仅因数量不足终止 M1–M6。
- 候选不足状态下，critic、可证伪性检查或 ranker 失败会降级保留当前候选并进行确定性排序。
- 如果完全没有通过任务契约的假设，仍使用原有的契约修复路径，不放宽证据和任务一致性硬约束。

### M6：只保留既有兼容性修复

- 不修改同学版本的评审、聚合和迭代决策流程。
- 保留我们此前的结构化输出兼容：当模型把 `reasoning` 或 `suggestions` 返回为字符串数组时，在状态边界将其合并为可展示文本，避免整轮因返回格式差异失败。

### UI 和运行接口

- M1 详情展示具体子问题、关键实体和传递给 M2 的任务约束。
- M2 详情展示各检索源命中/采纳数量、论文题目、全文/摘要状态和输出给 M3 的知识条目。
- M3 提供全屏证据图谱，并展示实体合并前后的审计记录。
- M4、M5、M6 分别展示具体假设、研究方案和评分/评审内容。
- 保留运行过程、Tool 事件、失败降级信息、追问迭代和断点重试记录。

## 明确未包含的内容

- 不提交任何 API Key 或 `.env`。
- 不提交 `output/`、`.cache/`、`knowledge_graph/` 等本地运行数据。
- 本次不继续修改 M5、M6 的算法质量问题；已知问题继续记录在 `TODO.md` 中，由后续提交处理。

## 验证范围

- M1 英文子问题与实体约束测试。
- M2 多源搜索、轮次规划、全文获取和导出测试。
- M3 图谱实体合并、边继承及合并审计测试。
- M4 任务契约注入、候选不足重试和降级继续测试。
- M5 提示词语言一致性测试。
- 网页后端事件、详细输出和凭据不落盘测试。
